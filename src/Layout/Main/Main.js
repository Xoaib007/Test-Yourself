import React from 'react';
import { Outlet } from 'react-router-dom';
import Header from '../../Componet/Header/Header';

const Main = () => {
    return (
        <div>
            
        </div>
    );
};

export default Main;